import 'package:flutter/material.dart';

class ExtendsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Extends Page'),
      ),
    );
  }
}
